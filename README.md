# orgset_transposer
Python CLI for transforming SAP Orgset (e.g. from XITING XAMS) CSV data into pivoted matrices (Orgsets as rows, fields as columns) with support for multi-value aggregation and custom empty field handling.
